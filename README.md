# Python_MySql_CRUD
This project demonstrates CRUD operations (Create, Read, Update, Delete) using MySQL on a Student table. It is designed to help understand basic database operations and SQL queries.  The database used is school, and it contains a table named student which stores student-related information.
